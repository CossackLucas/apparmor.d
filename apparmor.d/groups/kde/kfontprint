# apparmor.d - Full set of apparmor profiles
# Copyright (C) 2026 Łukasz Kozak <lakis.kozak@gmail.com>
# SPDX-License-Identifier: GPL-2.0-only

abi <abi/5.0>,

include <tunables/global>

  # ToDo: printer abstraction?

@{exec_path} = @{lib}/{,@{multiarch}/libexec/}kfontprint
profile kfontprint @{exec_path} {
  include <abstractions/base>
  include <abstractions/graphics>
  include <abstractions/kde-strict>
  include <abstractions/nameservice-strict>

  network inet stream,
  network inet6 stream,

  signal receive set=kill peer=systemsettings,

  @{exec_path} mr,

  /etc/cups/client.conf r,

  / r,
  /home/ r,

  owner @{HOME}/ r,

  owner @{user_cache_dirs}/ r,
  owner @{user_config_dirs}/ r,
  owner @{user_config_dirs}/kcminputrc r,
  owner @{user_config_dirs}/kwinrc r,
  owner @{user_config_dirs}/user-dirs.dirs r,

  owner @{PROC}/@{pid}/mountinfo r,

  /dev/tty r,

  # Silencer
  # ToDo: check it!
  deny owner @{HOME}/.* r,

  include if exists <local/kfontprint>
}

# vim:syntax=apparmor
